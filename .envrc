if [[ -s "$HOME/.nvm/nvm.sh" ]]; then
	source "$HOME/.nvm/nvm.sh"
	nvm use --silent >/dev/null || echo "direnv: warning: nvm couldn't activate the requested Node version from .nvmrc" >&2
else
	echo "direnv: warning: nvm not found at $HOME/.nvm/nvm.sh" >&2
fi
