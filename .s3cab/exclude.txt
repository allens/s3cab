# Exclude rules for backing up this repository with s3cab itself.
# Syntax: see doc/exclude.md. '/' is always the separator, '**/' spans whole
# directories, '*' matches within a single segment, and a trailing '/' marks a
# directory. (The walker always skips .s3cab/, so it needs no rule here.)

# Dependencies and VCS metadata
**/node_modules/
.git/

# Build output and packaged binaries (esbuild bundle + SEA executable)
build/
dist/

# Test, coverage, and temporary output
coverage/
test/.tmp*/
**/*.tmp

# Logs
**/*.log
